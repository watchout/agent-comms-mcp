schema_version: shirube-cell/v1
status: draft_candidate
cell_id: CELL-MCP-004
title: Output schema validation
risk_tier: R2
repo_id: watchout/agent-comms-mcp
objective: Draft output schema validation expectations for planners, diagnostics, evidence, and conveyor-facing results.
scope:
  allowed_paths:
    - .shirube/**
    - docs/**
    - README.md
  forbidden_paths:
    - bin/**
    - core/**
    - cli/**
    - server.ts
    - .github/workflows/**
non_goals:
  - No new runtime schema validator.
  - No CLI output behavior change.
  - No required check activation.
acceptance_criteria:
  - Versioned output schemas are identified for future work.
  - Failure behavior expectations are documented.
  - Enforcement remains warn-only.
required_gates:
  - Shirube command review
  - repository owner/domain designer confirmation
