schema_version: shirube-cell/v1
status: draft_candidate
cell_id: CELL-MCP-006
title: Audit log policy
risk_tier: R2
repo_id: watchout/agent-comms-mcp
objective: Draft audit log and evidence sink policy for queue, runtime, delivery, waiver, and gate decisions.
scope:
  allowed_paths:
    - .shirube/**
    - docs/**
    - README.md
  forbidden_paths:
    - bin/**
    - core/**
    - cli/**
    - server.ts
    - .github/workflows/**
non_goals:
  - No audit table migration.
  - No runtime audit writer change.
  - No post-merge workflow change.
acceptance_criteria:
  - Required audit events are identified.
  - GitHub-native durable evidence sinks are documented.
  - Enforcement remains warn-only.
required_gates:
  - Shirube command review
  - repository owner/domain designer confirmation
