schema_version: shirube-control-handoff/rapid-lite/v1
mode: rapid-lite
profile: hotel-lite
control_handoff_id: CH-AUN-RUNTIME-V2-CLAIM-DRYRUN-001
parent_ssot: watchout/agent-comms-mcp#794
framework_ref: watchout/ai-dev-framework@465db4863eb1fb5e248df74d46b29889da06cfc4
repo_spec_ref: .shirube/repo-spec.yaml
premise_ref: .shirube/specs/SPEC-MCP-AUN-V2V3-FOUNDATION-001.md
repo: watchout/agent-comms-mcp
repo_local_issue: https://github.com/watchout/agent-comms-mcp/issues/794
source_mirror_ref: .shirube/source-mirrors/control-issue.yaml
owner:
  role: repo_owner
  actor: watchout
implementation_actor: codex-implementation-bot
next_role: independent_audit
protected_surfaces:
  - runtime dry-run CLI surface
  - queue row read/evaluation
rps_scope:
  shirube_owns:
    - Report-only gate routing for this implementation Cell.
    - Exact-head evidence and owner decision requirements before ready or merge handling.
  shirube_does_not_own:
    - Live queue mutation in this Cell.
    - Lease writes, DB migrations, provider delivery, state-daemon behavior, workflows, branch protection, rulesets, or AUN activation.
cell:
  CELL-ID: CELL-MCP-AUN-RUNTIME-V2-CLAIM-DRYRUN-001
  cell_type: implementation
  risk_class: R1
  goal: Add runtime-v2 claim dry-run planning without live claim, DB mutation, lease write, or AUN activation.
  governance_route: R1 dry-run implementation with independent audit before owner exact-head decision.
  non_scope:
    - live claim
    - queue mutation
    - lease write
    - DB schema or migration
    - state-daemon behavior
    - provider delivery
    - MCP/tool behavior
    - active workflow changes
    - required check activation
    - branch protection or ruleset mutation
    - AUN activation
  allowed_paths:
    - bin/aun.ts
    - bin/aun/runtime-v2.ts
    - core/aun-runtime-v2-claim-plan.ts
    - tests/aun-runtime-v2-claim-plan.test.ts
  forbidden_paths:
    - server.ts
    - cli/**
    - adapters/**
    - db/**
    - .github/workflows/**
    - package.json
    - package-lock.json
    - bun.lockb
    - pnpm-lock.yaml
    - .env*
    - secrets/**
    - deploy/**
  protected_surfaces:
    declared:
      - runtime dry-run CLI surface
      - queue row read/evaluation
    touched: true
  stop_conditions:
    - live claim is required
    - queue mutation is required
    - DB schema or migration is required
    - exact fence cannot be enforced before DB open
    - missing --dry-run cannot fail before DB open
    - independent audit fails
implementation_boundary:
  summary: Add an exact-fenced, read-only claim dry-run planner for runtime-v2.
  expected_pr_count: 1
  forbidden_operations:
    - merge
    - approve
    - draft removal
    - live claim
    - queue mutation
    - lease write
    - DB migration
    - provider delivery
    - state-daemon mutation
    - AUN activation
validation:
  required_commands:
    - git diff --check
    - YAML parse for .shirube/**/*.yaml
    - bun test tests/aun-runtime-v2-claim-plan.test.ts tests/aun-runtime-v2-plan.test.ts tests/aun-runtime-v2.test.ts tests/run-queue-work-plan.test.ts tests/queue-work.test.ts tests/contract/test_aun_cli_real_invocation.test.ts
  required_evidence:
    - PR_head_SHA
    - changed_files
    - validation_commands
    - validation_results
    - independent_audit_result
owner_decision:
  required_before_merge: true
  evidence_source: external_owner_decision_comment_or_workflow_input
  committed_pending_policy_only: true
  allowed_verdicts:
    - APPROVED_EXACT_HEAD
    - CHANGES_REQUIRED
    - BLOCKED
  exact_head_sha: null
  decision_ref: null
post_merge:
  required_when_done_claimed: true
  required_evidence:
    - merge_commit
    - merged_at
    - post_merge_smoke_or_NA
    - next_step
