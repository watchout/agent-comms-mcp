schema_version: shirube-control-handoff/rapid-lite/v1
mode: rapid-lite
profile: hotel-lite
control_handoff_id: CH-AUN-CONNECTOR-CREDENTIAL-DIAGNOSTIC-001
parent_ssot: watchout/agent-comms-mcp#794
framework_ref: watchout/ai-dev-framework@cef1aae4815299855dc367609a84d9d8f892c716
repo_spec_ref: .shirube/repo-spec.yaml
premise_ref: .shirube/specs/SPEC-MCP-AUN-V2V3-FOUNDATION-001.md
owner_confirmation_ref: https://github.com/watchout/agent-comms-mcp/pull/805#issuecomment-4776374018
premise_confirmation_ref: https://github.com/watchout/agent-comms-mcp/pull/805#issuecomment-4776374018
spec_review_state: ready_for_implementation
repo: watchout/agent-comms-mcp
repo_local_issue: https://github.com/watchout/agent-comms-mcp/issues/794
source_mirror_ref: .shirube/source-mirrors/control-issue.yaml
owner:
  role: repo_owner
  actor: watchout
implementation_actor: codex-implementation-bot
next_role: independent_audit
protected_surfaces:
  - secrets
protected_surface_details:
  - Connector credential diagnostic implementation only.
  - Adds a read-only CLI diagnostic over existing credential evidence.
  - Uses DB evidence only and does not execute provider or token verification.
  - No active workflows, required checks, branch protection, or rulesets are changed in this implementation PR.
rps_scope:
  shirube_owns:
    - Report-only gate routing for this implementation Cell.
    - Exact-head evidence and owner decision requirements before ready or merge handling.
    - Audit checklist coverage for the connector credential diagnostic implementation PR.
  shirube_does_not_own:
    - Provider API calls, raw token resolution, credential mutation, channel access discovery, runtime activation, state-daemon behavior, branch protection, rulesets, or AUN activation.
cell:
  CELL-ID: CELL-MCP-AUN-CONNECTOR-CREDENTIAL-DIAGNOSTIC-001
  cell_type: implementation
  risk_class: R1
  goal: Add the smallest read-only connector credential diagnostic after runtime-v2 mediated finalization has merged.
  governance_route: R1 read-only diagnostic implementation with independent audit before owner exact-head decision.
  non_scope:
    - DB schema or migration
    - provider API calls
    - raw token resolution or token verification
    - credential row creation or mutation
    - channel access discovery
    - routing or delivery behavior change
    - MCP/tool behavior change
    - state-daemon behavior change
    - active workflow change
    - required check activation
    - branch protection or ruleset mutation
    - AUN activation or allowlist expansion
  allowed_paths:
    - bin/aun.ts
    - bin/aun/connector.ts
    - core/aun-connector-credential-diagnostic.ts
    - tests/aun-connector-credential-diagnostic.test.ts
    - .shirube/control-handoffs/CH-AUN-CONNECTOR-CREDENTIAL-DIAGNOSTIC-001.yaml
    - .shirube/audit-checklists/AUDIT-CHECKLIST-CELL-MCP-AUN-CONNECTOR-CREDENTIAL-DIAGNOSTIC-001.yaml
    - .shirube/evidence/EVIDENCE-CELL-MCP-AUN-CONNECTOR-CREDENTIAL-DIAGNOSTIC-001.yaml
  forbidden_paths:
    - server.ts
    - cli/**
    - adapters/**
    - db/**
    - core/state-daemon/**
    - package.json
    - package-lock.json
    - bun.lockb
    - pnpm-lock.yaml
    - .env*
    - secrets/**
    - deploy/**
  protected_surfaces:
    declared:
      - secrets
    touched: true
    details:
      - Implementation opens SQLite readonly/no-create in the CLI path.
      - "Implementation requires --json and emits applied_mutations: []."
      - Implementation does not print raw-secret-like token material.
      - Implementation uses DB evidence only and does not call provider APIs.
      - Report-only route metadata landed separately in PR #826.
  stop_conditions:
    - provider API call or raw token verification is required
    - credential creation, mutation, or migration is required
    - state-daemon, MCP/tool, delivery, branch protection, ruleset, or AUN activation work is required
    - independent audit fails
implementation_boundary:
  summary: Add a read-only connector credential diagnostic that reports credential evidence blockers/warnings without provider access or mutation.
  expected_pr_count: 1
  forbidden_operations:
    - approve
    - merge
    - draft removal
    - provider API calls
    - raw token resolution
    - credential mutation
    - channel access discovery
    - DB migration
    - state-daemon mutation
    - AUN activation
acceptance_criteria:
  - CLI command is aun connector credential-diagnostic [--agent-id <id>] [--provider discord] --json.
  - Output schema is aun-connector-credential-diagnostic/v1.
  - Healthy connector credential evidence reports zero blockers.
  - Raw-secret-like refs are redacted and reported.
  - Duplicate active secret refs are diagnosed.
  - Connector/credential owner mismatch and missing usable credential evidence are diagnosed.
  - CLI smoke emits JSON from an existing SQLite DB opened readonly/no-create.
  - "Successful diagnostic output includes applied_mutations: []."
validation:
  required_commands:
    - git diff --check
    - YAML parse for .shirube/**/*.yaml
    - node --check scripts/shirube-current-overlay-check.mjs
    - bun test tests/aun-connector-credential-diagnostic.test.ts
  required_evidence:
    - PR_head_SHA
    - changed_files
    - validation_commands
    - validation_results
    - audit_checklist_report
owner_decision:
  required_before_merge: true
  evidence_source: external_owner_decision_comment_or_workflow_input
  committed_pending_policy_only: true
  allowed_verdicts:
    - APPROVED_EXACT_HEAD
    - CHANGES_REQUIRED
    - BLOCKED
  exact_head_sha: null
  decision_ref: null
post_merge:
  required_when_done_claimed: true
  required_evidence:
    - merge_commit
    - merged_at
    - post_merge_smoke_or_NA
    - next_step
