schema_version: "shirube-machine-evidence/v1"
evidence_id: "EVIDENCE-CELL-MCP-AUN-CONNECTOR-CREDENTIAL-DIAGNOSTIC-001"
target_repo: "watchout/agent-comms-mcp"
target_pr: null
cell_id: "CELL-MCP-AUN-CONNECTOR-CREDENTIAL-DIAGNOSTIC-001"
implementation_actor: "codex-implementation-bot"
evidence_scope: "connector_credential_read_only_diagnostic_implementation"
not_structured_audit: true
owner_decision_required_before_merge: true
handoff_ref: ".shirube/control-handoffs/CH-AUN-CONNECTOR-CREDENTIAL-DIAGNOSTIC-001.yaml"
audit_checklist_ref: ".shirube/audit-checklists/AUDIT-CHECKLIST-CELL-MCP-AUN-CONNECTOR-CREDENTIAL-DIAGNOSTIC-001.yaml"
claims:
  implementation_summary:
    - "Future implementation adds aun-connector-credential-diagnostic/v1 read-only diagnostic."
    - "Future implementation adds aun connector credential-diagnostic [--agent-id <id>] [--provider discord] --json."
    - "Future implementation uses DB evidence from agents, connector_instances, and connector_credentials."
    - "Future implementation redacts raw-secret-like token material."
    - "Future implementation reports blockers/warnings without provider API calls or DB mutation."
  protected_surface_boundary:
    runtime_code_changed: false
    database_mutation_code_changed: false
    db_schema_or_migration_changed: false
    provider_api_called: false
    raw_secret_resolution: false
    credential_mutation: false
    channel_access_discovery: false
    state_daemon_changed: false
    provider_delivery_changed: false
    mcp_tool_behavior_changed: false
    active_workflow_changed: true
    required_check_changed: false
    branch_protection_or_ruleset_changed: false
    aun_activation: false
  fail_closed_guards:
    - "SQLite CLI path must use readonly/no-create DB open."
    - "Diagnostic DB wrapper must reject mutation SQL."
    - "Raw-secret-like refs must be redacted."
    - "Successful diagnostics must return applied_mutations: []."
commands:
  - command: "git diff --check"
    result: "PENDING"
  - command: "YAML parse for .shirube/**/*.yaml"
    result: "PENDING"
  - command: "node --check scripts/shirube-current-overlay-check.mjs"
    result: "PENDING"
  - command: "bun test tests/aun-connector-credential-diagnostic.test.ts"
    result: "PENDING"
validation_results:
  - "PENDING"
evidence_refs:
  - "PR_head_SHA"
  - "changed_files"
  - "validation_commands"
  - "validation_results"
  - "audit_checklist_report"
notes:
  - "This file is the machine-evidence template for the connector credential diagnostic implementation PR."
  - "It is not a structured audit and does not approve ready, merge, owner decision, provider token verification, credential mutation, or AUN activation."
  - "The route metadata PR that introduces this file changes only report-only Shirube routing metadata."
