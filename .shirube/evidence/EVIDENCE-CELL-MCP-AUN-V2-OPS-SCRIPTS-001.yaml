schema_version: "shirube-machine-evidence/v1"
evidence_id: "EVIDENCE-CELL-MCP-AUN-V2-OPS-SCRIPTS-001"
target_repo: "watchout/agent-comms-mcp"
target_pr: 861
cell_id: "CELL-MCP-AUN-V2-OPS-SCRIPTS-001"
implementation_actor: "aun"
evidence_scope: "v2_operational_scripts"
not_structured_audit: true
owner_decision_required_before_merge: true
handoff_ref: ".shirube/control-handoffs/CH-AUN-V2-OPS-SCRIPTS-001.yaml"
audit_checklist_ref: ".shirube/audit-checklists/AUDIT-CHECKLIST-CELL-MCP-AUN-V2-OPS-SCRIPTS-001.yaml"
claims:
  implementation_summary:
    - "Round-trip bench: throwaway-DB-only guard; measured 50 rounds/462ms, per-leg 4.6ms, zero anomalies (2026-07-09)."
    - "Live pilot: exact-fenced import + real engines + typed notify delivery; production PASS on 3 real seats (#794 comment 4924913779)."
  protected_surface_boundary:
    startup_or_session_policy_changed: false
    active_workflow_changed: true
    active_workflow_change_scope: "route-table mapping for this Cell only"
    runtime_code_changed: false
    queue_or_db_mutation: false
    db_schema_or_migration_changed: false
    launchd_or_launchctl_changed: false
    secrets_or_deployment_changed: false
    required_check_changed: false
    branch_protection_or_ruleset_changed: false
    production_activation: false
    audit_qa_check_cto_owner_substitution: false
  fail_closed_guards:
    - "Bench exits refusing the production database by name."
    - "Pilot processes only explicitly passed queue ids; V1 rows are never mutated by the scripts."
    - "Owner exact-head decision remains required before merge."
commands:
  - command: "git diff --check"
    result: "PASS"
  - command: "YAML parse for .shirube/**/*.yaml"
    result: "PASS"
  - command: "bun build --target bun scripts/bench-v2-roundtrip-pg.ts scripts/v2-live-pilot.ts"
    result: "PASS"
validation_results:
  - "build check pass for both scripts ON TOP OF PR #860 (stacked; scripts import core/eventlog worker/runtimes from #860)"
  - "production evidence recorded on #794"
evidence_refs:
  - "PR_head_SHA"
  - "changed_files"
  - "validation_commands"
  - "validation_results"
notes:
  - "Machine evidence only; not a structured audit; approves nothing."
